services:
  - type: web
    name: banknote-streamlit
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m streamlit run src/app.py --server.port $PORT --server.address 0.0.0.0
